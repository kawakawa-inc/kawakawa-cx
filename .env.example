# Database Configuration
DATABASE_URL=postgresql://kawa:kawa_dev_password@localhost:5432/kawa_market

# FIO API (optional - only needed for private endpoints)
FIO_API_KEY=

# Application
NODE_ENV=development
PORT=3000

# OpenSearch (production log search + `make logging-*` targets)
# Full connection URL including credentials, from the DigitalOcean console.
PROD_OPENSEARCH_URL=
